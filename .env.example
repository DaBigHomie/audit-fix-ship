# Local only — copy to .env (never commit .env)

# Copilot /exit and exit-session.mts: short post via Incoming Webhook (channel is whatever Slack bound to this URL).
# Separate from bot-token flows used elsewhere (xoxb + channel ID).
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/XXX/YYY/ZZZ
